[client]
default-character-set=utf8mb4

[mysqld]
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

# スロークエリログの出力無効
slow_query_log = 0
# スロークエリログの出力を行う場合は以下3行のコメントアウトを外す
# slow_query_log = 1
# slow_query_log_file = /var/log/mysql/mysql-slow.log
# long_query_time = 3

# 開発環境はbinログの出力をしない
disable-log-bin
